# from django.utils import timezone
# from webapp.models import *
# import datetime
#
# Tracker.objects.filter(status ='2', create_at_lte = timezone.now() - datetime.timedelta(days=30))
# Tracker.objects.filter(project = '1').values('type.id')
# Tracker.objects.filter(summary__icontains= 'a').values('project_id')
